# Example SMTP settings for contact emails
# Set these in a real .env file (never commit real secrets)
SMTP_HOST=smtp.example.com
SMTP_PORT=587
SMTP_USER=your-smtp-user@example.com
SMTP_PASS=your-smtp-password

# Optional: From and To addresses used in contact emails
CONTACT_FROM_EMAIL=contact@yourdomain.com
CONTACT_TO_EMAIL=support@yourdomain.com

# Other existing env vars used by the server
MONGO_URI=
PORT=5000
MONGO_URI=mongodb://localhost:27017/ecommerce
JWT_SECRET=your_jwt_secret_here
PORT=5000
